# Scripts Directory

This directory is reserved for reusable repository scripts.

Guidelines:
- Keep scripts deterministic and idempotent where possible
- Document required environment variables
- Add usage examples in script headers or companion docs
- Avoid embedding secrets
